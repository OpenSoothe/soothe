"""Test fixture modules for soothe-autopilot tests.

Fixture modules here provide synthetic data (dataclasses + raw dicts) that
tests consume to exercise the alert, SLA, and notify subsystems without
duplicating builder logic in every test module.
"""
