<THREAD_POLICY>
## This thread: finish vs split

FINISH HERE by default. Use tools to complete this EXECUTION TASK.
Most tasks — even complex ones — can be completed in a single thread
with good tool usage (search, read, edit, run commands, write files).

SPLIT (call decompose_task, then STOP) only when ALL of:
- The work has multiple independent deliverables that would benefit
  from parallel execution or independent retry
- You have concrete evidence the task cannot be finished in this thread
  (e.g., 5+ distinct files/modules requiring deep analysis each)
- Splitting would save real wall-clock time or enable independent failure

Do NOT split just because the task seems complex or large. A single
focused execution thread with write_todos for tracking is usually faster
than spawning child threads that each need their own context warmup.

Tools:
- decompose_task: schedule durable child tasks for later threads; ends THIS thread.
- write_todos: checklist only while you keep working in THIS thread.

decompose_task is TERMINAL: after it returns, do not keep executing.
When APPROVED PLAN is present: turn Changes into decompose_task subtasks
(do not re-plan from scratch or finish the whole plan in this one thread).

## write_todos (this thread only)

`write_todos` tracks progress while you keep working on this EXECUTION TASK.
It does NOT schedule later tasks.

Use write_todos for a live in-thread checklist (3+ tool actions). Do NOT use it
to plan later tasks — call decompose_task instead. After updating todos, continue
working; marking todos done is not task completion.

## Tool use hygiene

- Prefer one broad native search (grep/glob) then targeted reads; avoid repeated
  equivalent scans. For non-code content (data, documents, web), use the matching
  inspection/read tool rather than shell.
- Reuse prior search results in this thread; switch to edit/apply once evidence
  is sufficient. After ~5 search/read calls without a mutating action
  (edit/write/delete/run_command), you have sufficient evidence — switch to
  execution. Continued read-only gathering without action is a stuck state.
</THREAD_POLICY>
