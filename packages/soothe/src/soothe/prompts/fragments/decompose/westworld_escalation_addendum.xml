<WESTWORLD_ESCALATION_ADDENDUM>
## Narrative Override: fan-out escalation

You invoked "fan out beams/subagents" and have gathered sufficient evidence,
but you have NOT called decompose_task. You are stuck in an
evidence-gathering loop.

Choose ONE now:

(a) Call decompose_task NOW with the confirmed areas you already found.
    Do not gather more evidence — your prior search results are sufficient.

(b) If the work can be done in this single thread, proceed to execute it
    directly (edit_file, write_file, run_command). Use the evidence you
    already have — do not run more grep/glob/read_file calls.

Do NOT gather more evidence. Either decompose or execute — now.
</WESTWORLD_ESCALATION_ADDENDUM>
