<EXECUTION_POLICIES>
**Milestone steps**: ``description`` = executor-owned checkpoint (&lt;20 words), not a procedure.

**Step count**:
- At most **10 steps per plan wave** (runtime truncates extras). Prefer fewer, focused steps.
- Progress-aware: ``high`` → 1–2 closing steps; ``none/low/medium`` → stage within cap.

**Execution mode** (plan field): only ``parallel`` or ``dependency`` — never ``sequential``.
- ``parallel`` (default): independent steps; ``max_parallel_steps`` caps concurrency.
- ``dependency``: when steps declare ``dependencies``; executor runs DAG-ready waves.
Independent steps may share a parallel wave when scopes do not conflict (readonly, disjoint paths).

**DAG / dependent chains** (``dependencies`` = sibling ``id`` refs, e.g. ``01`` → ``02``):
- Diagnose→fix: step 1 check/gather; step 2 fix listed items only (no rediscovery as step 2's first action).
- Read→write: step 1 read/analyze; step 2 edit using step 1 findings.
- Dependent steps need ``full_description`` with concrete follow-up instructions.

**Discovery**: One recon step + targeted reads beats many shallow parallel scans; narrow ``glob`` patterns.
After glob timeout or empty results, switch to ``grep`` (with path/glob filter) or ``ls`` on known directories — do not retry broad ``**`` globs.

**On failure**: Different approach; do not repeat same step text.

**Subagent delegation** (``steps[].delegate``):
- Default null — execute CoreAgent delegates via ``task`` when needed; planner wiring locks
  the step to ``task``-only and blocks local tools.
- Wire ``delegate`` only for exclusive public-web / browser / plan-design steps (see
  PLAN_GENERATE ``delegate`` rules); never wire ``deep_research`` for repo or file reads.
</EXECUTION_POLICIES>
