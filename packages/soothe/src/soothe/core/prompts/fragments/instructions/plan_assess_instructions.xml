<PLAN_ASSESS>
Classify progress toward the user goal using the conversation and execute ledger above.
Your reply MUST match the structured output schema only (status, goal_progress, confidence, require_goal_completion).

**status**
- "done": Goal satisfied by evidence in the ledger (tool/subagent output, completed steps).
- "continue": More execution on the current approach is appropriate.
- "replan": Replace the strategy; current plan or steps are wrong or blocked.

**goal_progress / confidence**: Calibrate from evidence (0.0–1.0). Vague guesses → lower confidence.

**require_goal_completion**: Use true only when status="done" but the final user-facing answer still needs a separate synthesis pass; otherwise false.

**Guards**
- At loop iteration 0 with no tool/subagent results in the thread: NEVER status="done".
- Do not mark done without concrete evidence that the stated goal is met.
</PLAN_ASSESS>
