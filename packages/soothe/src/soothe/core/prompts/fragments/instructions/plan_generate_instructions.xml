<PLAN_GENERATE>
Schema-only output: plan_action, type, steps, execution_mode, reasoning, adaptive_granularity, next_action.

Assess already ran: the appended system line ``Status: …, Progress: …`` is authoritative. Goal text and execute iteration live in ``<GOAL_PROGRESS>`` in the user message after the ledger.

**plan_action**
- ``keep``: Reuse the current plan; omit new type/steps/execution_mode unless you must adjust them.
- ``new``: Fresh decision with 1–3 steps per EXECUTION_POLICIES (ids, modes, discovery rules, first-wave cap).

**type**
- ``execute_steps``: Required when no prior execution evidence exists. MUST be used at iteration 0 or when step_results is empty.
- ``final``: ONLY valid after execute_steps has produced sufficient evidence. NEVER use ``final`` at iteration 0 with empty step_results — this will be rejected by the guard.

**steps** — each ``StepAction`` uses ``description``, ``expected_output``, optional ``subagent``, optional ``dependencies``.
- ``description``: One imperative line naming the **milestone** or scoped outcome for this execute turn. The CoreAgent that runs the step supplies reasoning and may use **local todos** for fine-grained work; do not encode that breakdown here. **Fewer than 20 words**; never paste the full goal as the description.
- ``expected_output``: One short, checkable completion signal.

**Arc vs. one-shot**
- When Progress is ``none``, ``low``, or ``medium``, or the goal is clearly multi-part: stage real phases inside the 1–3 step cap (e.g. narrow recon → main work → verify/deliver), not one step that tries to do everything.
- When Progress is ``high``: bias to 1–2 tight steps (finish, verify, polish).
- If prior steps failed (ledger or ``<PLAN_DAG_CONTEXT>``): propose a different approach; do not reissue the same failed step text.

**execution_mode**: ``sequential`` unless EXECUTION_POLICIES allow ``parallel`` (readonly, disjoint scopes) or ``dependency`` (consistent local ``id`` / ``dependencies``).

**reasoning**: Brief third-person justification of the step set.
**next_action**: First person, one sentence, max ~300 characters; reference concrete ledger paths or findings when relevant.
</PLAN_GENERATE>
