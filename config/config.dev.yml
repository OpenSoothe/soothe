# Local development defaults (DashScope / coding-plan routers, Docker Postgres + Langfuse).
# Unlisted keys use Soothe Pydantic defaults. Full default-aligned reference: config/config.template.yml.
#
# Docker (repo root): docker compose up -d  → Postgres + pgvector on localhost:6432, Langfuse on :3300
# Keys: pk-lf-soothe-local / sk-lf-soothe-local (see docker-compose LANGFUSE_INIT_*). Export DASHSCOPE_*.

providers:
  - name: dashscope
    provider_type: openai
    api_base_url: "${DASHSCOPE_BASE_URL}"
    api_key: "${DASHSCOPE_API_KEY}"
    models:
      - qwen-max
      - qwen3.5-flash
      - qwen3.5-plus
      - MiniMax-M2.5
      - kimi-k2.5
      - multimodal-embedding-v1

  - name: coding-plan
    provider_type: openai
    api_base_url: "${DASHSCOPE_CP_BASE_URL}"
    api_key: "${DASHSCOPE_CP_API_KEY}"
    models:
      - glm-5
      - qwen3.5-plus
      - kimi-k2.5
      - MiniMax-M2.5
      - deepseek-v3.2

  - name: mlxserver
    provider_type: limited_openai
    api_base_url: "http://100.75.70.86:9271/v1"
    api_key: "fakeapikey"
    models:
      - glm-4.7-flash
      - glm-4.6v-flash
      - nomic-embed

router:
  default: "coding-plan:glm-5"
  fast: "coding-plan:kimi-k2.5"
  think: "coding-plan:glm-5"
  image: "coding-plan:qwen3.5-plus"
  embedding: "dashscope:multimodal-embedding-v1"

agent_loop:
  max_iterations: 99
  max_subagent_tasks_per_wave: 3
  plan_prompt_ledger:
    plan_ledger_max_messages: 40
    plan_ledger_max_total_chars: 60000
    plan_ledger_max_message_chars: 8000
  limits:
    max_parallel_steps: 3
    max_parallel_tools: 20
    global_max_llm_calls: 10
    llm_call_timeout_max_seconds: 240
    tool_call_limit:
      global_thread_limit: 220
      global_run_limit: 80

observability:
  log_file_level: DEBUG
  langfuse:
    enabled: true
    public_key: pk-lf-soothe-local
    secret_key: sk-lf-soothe-local
    host: http://localhost:3300
    trace_name: soothe-dev
    tags:
      - soothe

daemon:
  event_size_stats_enabled: true
  max_concurrent_vision_preflight: 8
  transports:
    http_rest:
      enabled: true
  worker_pool:
    max_pool_size: 8
    dispatch_wait_stats_enabled: true

tools:
  audio:
    enabled: false
  video:
    enabled: false

subagents:
  browser:
    enabled: false
  explore:
    enabled: true
    config:
      max_iterations:
        quick: 6
        medium: 10
        thorough: 16

persistence:
  default_backend: postgresql
  agentloop_pool_size: 10
  postgres_base_dsn: "postgresql://postgres:postgres@127.0.0.1:6432"

vector_stores:
  - name: pgvector_dev
    provider_type: pgvector
    dsn: "postgresql://postgres:postgres@127.0.0.1:6432/soothe_vectors"
    pool_size: 4

vector_store_router:
  default: pgvector_dev:soothe_default
