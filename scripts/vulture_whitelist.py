"""Vulture whitelist — known false positives and tracked dead-code debt.

Regenerate (then review diff) with::

    make vulture-whitelist

Each entry suppresses a specific high-confidence finding until the code is
fixed or removed.
"""
